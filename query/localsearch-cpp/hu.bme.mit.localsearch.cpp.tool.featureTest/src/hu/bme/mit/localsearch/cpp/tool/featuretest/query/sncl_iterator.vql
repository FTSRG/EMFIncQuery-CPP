package hu.bme.mit.localsearch.cpp.tool.featuretest.query
/*
import "http://arch.ecore/model"

@Bind(parameters={S1})
pattern linked(S1 : SN,S2 : SN){
	SN.link(S1,S2);
} or {
	SN(S1);
	SN(S2);
	SN.link(S2,S1);
}

pattern pconnected(C1,C2){
	SN(S1);
	CL.cnn(C1,S1);
	CL.cnn(C2,S1);
} or {
	CL.cnn(C1,S1);
	CL.cnn(C2,S2);
	find linked+(S1,S2);
}

@Bind(parameters={C1})
pattern isolated(C1 : CL){
	neg find pconnected(C1,_C2);
}*/